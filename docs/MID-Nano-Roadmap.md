# MID Nano 文档与功能查缺补漏（对照示例手册）

本文件用于对照 `docs/example/MInDes-Guide.md` 的结构与内容，对 MID Nano 当前状态进行查缺补漏，并列出后续可执行的改进项（不等同于承诺交付）。

---

## 文档层面（优先）

### 手册结构对齐
- [x] 增加完整手册草案：`docs/MID-Nano-Guide.md`（含截图占位与标注说明）
- [ ] 统一术语与命名：界面名称（Experiment/Visualizer）、参数名（u0/con0/steps/mod/seed 等）
- [ ] 增加“版本信息/更新日志”表格：建议自动从 tag 生成，或手动维护一份简表

### 现有文档一致性
- [x] 更新 `docs/usage.md` 与 `docs/gui_tabs.md`：与当前 UI 保持一致（特别是 Experiment 的 Fixed/Range/List 模式、Visualizer 的 Plot/Table 逻辑、缩放/拖动方式）
- [x] 更新 `README.md` 的功能描述：避免继续写成 “仅 u0/con0/steps 扫参”

### 截图与标注规范
- [ ] 建议建立 `docs/images/`：按“页面-区域-版本号”命名截图，便于更新维护
- [ ] 约定标注风格：①②③ 编号 + 统一颜色/字体（便于论文/汇报复用）

---

## 产品层面（对齐示例但可按需取舍）

### “关于/信息”模块
- [ ] 增加 About 页面/对话框：版本号、构建信息、仓库地址、依赖说明（FFTW/Qt）、许可信息（如有）
- [ ] 增加一键复制诊断信息：便于反馈问题（系统信息、版本、最近一次命令行等）

### 项目/任务管理（可选）
示例手册强调“项目导航/项目管理”。MID Nano 当前以“输出目录”为核心，若要进一步对齐，可考虑：
- [ ] “项目”概念：项目根目录 + 多次 run 的组织、备注与标签
- [ ] 任务队列管理：暂停/继续、失败重试、跳过当前组合、导出任务列表

---

## 可视化与结果导出（重点建议）

### Plot（热图）完善
- [ ] 颜色条（Colorbar）与数值范围显示：当前仅渲染颜色，建议补充 min/max、单位说明
- [ ] 鼠标悬停信息：显示 x/y/value（或 step）以便对点位做解释
- [ ] 多种调色板：jet/viridis/gray 等，满足论文配图需求

### Table（表格）完善
- [ ] 支持复制选中行/列到剪贴板（CSV）
- [ ] 支持导出 CSV（对 run_config/timestamps/通用 txt 都适用）

---

## 构建与发布（工程化）

### Release 产物形态
示例文档包含“下载/校验/安装”。MID Nano 已有 GitHub Actions 自动构建，但仍有优化空间：
- [ ] 产物命名规范化：建议去掉空格或同时提供无空格别名（更利于脚本下载）
- [ ] Windows 可选提供安装包（MSI）/自解压
- [ ] macOS 可选提供 DMG，并完善签名/公证（如需分发给更广泛用户）
- [ ] Linux 可选提供 AppImage 或 deb/rpm（按需要简化）

### 质量保障
- [ ] 增加最小化的 CI 测试：CLI `--help`、GUI 资源加载检查、示例输出解析单测（只测解析逻辑即可）
- [ ] 增加示例输出（小数据）：用于文档截图与回归验证（注意 `.gitignore` 当前忽略 `*.txt`，需要策略调整）

---

## 许可与合规（视需求）

示例手册有明确“声明/许可/合作联系”。MID Nano 若计划对外发布或商用，建议明确：
- [ ] 增加 LICENSE（如 MIT/BSD/GPL/私有许可等）
- [ ] 明确第三方依赖许可（Qt/FFTW/OpenMP 等）
- [ ] 增加隐私说明：本项目是否会联网、是否收集任何数据（通常为“不收集”）
